apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httpbin
spec:
  parentRefs:
    - name: gateway
      namespace: gateway-api
  hostnames:
    - "httpbin.concordalabs.com"
  rules:
    - matches:
      backendRefs:
        - name: httpbin
          port: 3000
